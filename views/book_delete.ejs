<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head"); %>
  </head>
  <body class="container-fluid">    
    <div class="row">
      <div class="col-sm-2">
        <header>
          <%- include("partials/nav"); %>
        </header>
      </div>
      <div class="col-sm-10">
        <h1><%- title %>: <%- book.title %></h1>
        <p>By <strong><%- book.author.name %></strong></p>
        <% if (bookinstances.length > 0) { %>
            <p><strong>Delete the following book instances before attempting to delete this book.</strong></p>
            <div style="margin-left: 20px; margin-top: 20px">
                <h4>Book Instances</h4>
                <dl>
                    <% for (var i = 0; i < bookinstances.length; i++) { %>
                        <dt>
                            <a href="<%= bookinstances[i].url %>"><%- bookinstances[i]._id %></a>
                        </dt>
                        <dd><%- bookinstances[i].imprint %></dd>
                    <% } %>
                </dl>
            </div>
        <% } else { %>
            <p class="text-danger"><strong>Do you really want to delete this book?</strong></p>
            <form method="post" action="">
                <div class="form-group">
                    <input id="bookid" class="form-control" type="hidden" name="bookid" required="true" value="<%- book._id %>">
                </div>
                <button class="btn btn-primary" type="submit">Delete</button>
            </form>
        <% } %>
      </div>
    </div>
    <footer>
      <%- include("partials/footer"); %>
    </footer>
  </body>
</html>
