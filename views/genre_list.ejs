<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head"); %>
  </head>
  <body class="container-fluid">    
    <div class="row">
      <div class="col-sm-2">
        <header>
          <%- include("partials/nav"); %>
        </header>
      </div>
      <div class="col-sm-10">
        <h1><%= title %></h1>
        <ul>
            <% if (genre_list.length > 0) { %>
                <% for (var i = 0; i < genre_list.length; i++) { %>
                    <li>
                        <a href="<%= genre_list[i].url %>"><%= genre_list[i].name %></a>
                    </li>
                <% } %>
            <% } else { %>
                <li>No genres available.</li>
            <% } %>
        </ul>
      </div>
    </div>
    <footer>
      <%- include("partials/footer"); %>
    </footer>
  </body>
</html>
